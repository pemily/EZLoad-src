package com.pascal.ezload.service.dashboard.config;

public enum ChartPerfGroupedBy {
    FROM_START, // depuis le début du graphique
    MONTHLY, // depuis le début du mois
    YEARLY, // depuis le début de l'année calendaire
}
